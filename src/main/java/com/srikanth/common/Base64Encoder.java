package com.srikanth.common;

public class Base64Encoder {
	/*
	 * public static String base64EncryptData(String Data) { try { byte[] data =
	 * Data.getBytes("UTF-8"); byte[] mData = Base64.encode(data); String mstring =
	 * new String(mData); return mstring; } catch (UnsupportedEncodingException e) {
	 * e.printStackTrace(); return ""; }
	 * 
	 * }
	 * 
	 * public static String base64DecryptData(String Data) { try { // Base64 decoder
	 * = new Base64(); byte[] data = Data.getBytes("UTF-8"); byte[] decodedBytes =
	 * Base64.decode(data); // System.out.println(new String(decodedBytes) + "\n") ;
	 * String mstring = new String(decodedBytes); return mstring; } catch
	 * (UnsupportedEncodingException e) { e.printStackTrace(); return ""; } }
	 * 
	 * public static void main(String[] args) {
	 * System.out.println("Base64Encoder.main()" +
	 * base64DecryptData("c3Jpa2FudGg=")); }
	 */
}